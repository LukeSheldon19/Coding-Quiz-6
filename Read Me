The time complexity of Plus Minus is O(n) as it is only dependent on a single for loop iterating over a List of length n. The space complexity is Constant O(1), as the space taken remains constant regardless of 
the input size.
